using UnityEngine;

public class OCMission : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       MainMenu.WhatRaceisMission = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
